package com.example.groupsSystem.repositories.requests;

import com.example.groupsSystem.models.requests.RequestForPublicPost;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestForPublicPostRepository extends JpaRepository<RequestForPublicPost, Integer> {
}
